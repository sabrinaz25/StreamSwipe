# Shared (optional)

Small place for shared constants/types between `apps/mobile` and `apps/api`.

This repo currently keeps mobile and API types separate to avoid build-tool coupling during a hackathon.
